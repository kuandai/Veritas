# Conan profiles

Place repository-specific profiles here and install them via:

```bash
conan config install conan/config
```

Example profile naming convention:
- `profiles/linux-x86_64`
- `profiles/linux-clang`
- `profiles/ci-release`
